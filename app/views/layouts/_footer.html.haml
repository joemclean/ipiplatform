-if current_user
  .well.footer
    .btn-group
      -if current_user.is_admin
        .btn
          =link_to 'User Accounts', users_path
      .btn
        =link_to 'Resources', resources_path
      .btn
        =link_to 'Value Proposition Categories', pages_value_proposition_categories_path
