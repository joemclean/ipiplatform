%p#notice= notice

%h2.resourcetitle
  ='Resource: '+ @resource.name
  %br
%p
  =image_tag 'ResourcePlaceHolder.png'


.row.showresource
  .col.left.span4{:style => 'font-size: 20px;'}
    = @resource.description
    %br
    %br
    %p.authorBox='Uploaded by: ' + @resource.author.name
    %br
    %b This is a great resource for:
    %b
      -@resource.traits.each do |trait|
        .btn.traitconnection
          =link_to trait.name.pluralize, trait_path(trait)
  .col.right.span8
    %b Full Description:
    {:style => 'font-size: 14px; margin: 0 5 0 10px;'}
    = @resource.full_description

%p
  -unless @resource.user_upvote(current_user).count > 0
    .btn
      = link_to 'Upvote', resource_upvotes_path(@resource), :method => :post
  -else
    .btn.btn-success
      = link_to 'Upvoted!', clear_upvote_path(resource_id: @resource.id) 
%p
%p
  -unless @bookmark.exists?
    .btn
      = link_to 'Bookmark', make_bookmark_path(resource_id: @resource.id)
  -else
    .btn.btn-success
      =link_to 'Remove Bookmark', unmake_bookmark_path(resource_id: @resource.id)
%p
  %b Link:
  = link_to @resource.link, @resource.link
%p
  %b Full Description:
  = @resource.full_description
  %p
    %b This is a great resource for:
    %ul
      -@resource.traits.each do |trait|
        %li
          =link_to trait.name.pluralize, trait_path(trait)
    %b With a focus in the following industries:
    %ul
      -@resource.industries.each do |industry|
        %li= industry.name

.btn-group
  .btn
    = link_to 'All Resources', resources_path

-if current_user
  .btn-group
  .btn
    = link_to 'Edit Resource', edit_resource_path(@resource)
-else
  .row.phase-white
    %p{:align => 'left', :style => 'color: #de403c; font-size: 1.5em; margin: 50 0 100 50;'} Log in to edit resources!
