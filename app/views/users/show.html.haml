.hero.signin_hero
  .container
    %h2
      =@user.name
    .image-group
      =image_tag @user.avatar_url
  %br

%h3
  - if current_user.present? && current_user.is_admin?
    .btn
      = link_to 'Manage Value Propositions and Colors Together', value_propositions_path, id: 'manage_value_propositions_and_colors'
    %br
    .btn
      = link_to 'Manage Colors', colors_path, id: 'manage_value_propositions'
    %br
    .btn
      = link_to 'Manage Resources', resources_path

%h3 Uploaded Resources
- @published_resources.each do |resource|
  .resourcebox.span12
    %strong= resource.name
    %br
    = resource.description
    %br
    .btn
      = link_to 'Show', resource
    - if current_user.can_edit_and_delete_resource?(current_user, resource)
      .btn
        = link_to 'Edit', edit_resource_path(resource.id)
      .btn
        = link_to 'Delete', resource, :method => :delete, :data => { :confirm => 'Are you sure?' }
    %br   

%h3 Bookmarked Resources
- @bookmarks.each do |bookmark|
  .resourcebox.span12
    %strong=bookmark.resource.name
    %br
    = bookmark.resource.description
    %br
    = link_to 'Show', bookmark.resource
    %br


